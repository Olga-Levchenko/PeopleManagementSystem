---
epic: 2
date: 09-12-2026
verdict: accepted-with-open-items
criteria: profiled
headless: false
---

# Epic 2 Retrospective: All Employees List & Self-Service

## Epic summary

**Epic:** 2 — All Employees list, export, colleague mode, employee profile self-service, and manager/PP read surfaces on `EmployeeProfilePage`.

**Diff range:** `5a3b0bd^..ae53096` (29 commits on first-parent spine; merges measured: 2). Source: `git_evidence.py` run 2026-09-12.

**Stories completed (11):** 2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 2.6b, 2.7, 2.8, 2.9, 2.10 — all `done` in `sprint-status.yaml`; `pending_stories: []` from `detect-epic --epic 2`.

**Delivery PRs (main):** #65–#90 (implementation #65, #67, #69, #72, #74, #77, #79, #81, #87, #85, #90).

**Evidence inventory**

| Artifact | Status |
|----------|--------|
| Epic definition + per-story AC | `_bmad-output/planning-artifacts/epics.md` § Epic 2 |
| Story specs | `_bmad-output/implementation-artifacts/spec-2-*.md` (all `done`) |
| Git diff / commit volume | `git_evidence.py` — top churn: `employees.service.ts` (+987 net), `profile.e2e-spec.ts` (+627), `EmployeeProfilePage.tsx` (+348), `profile.service.spec.ts` (+513) |
| Sprint status | Current; retro key was `optional` |
| Previous epic retro | None (`epic-1-retro-*.md` absent) |
| Session logs | Not systematically archived — process lessons rely on specs, PRs, deferred-work |

**Notable artifact event:** chore PR #83 marked Epic 2 `done` before stories 2.6b/2.9/2.10 landed; corrected when follow-up stories shipped (#84–#90).

## Findings

### Wins (with sources)

1. **Shared access-control spine held.** `profile-audience.util.ts` and `employees.service.ts` reuse Epic 1 batch ACS resolution; colleague whitelist, project-line narrowing, and manager/PP merges extended without hardcoded role checks. Source: `profile-audience.util.ts`, `section-matrix.md` traces updated per story.

2. **Incremental manager profile read worked.** 2.8 (shell) → 2.6/2.7 (self) → 2.9 (S4/S9) → 2.6b (S3/S5/uploads) → 2.10 (manager S3/S5 + S1 photo UI). Each story mirrored the prior wiring pattern. Source: story specs + merge sequence `5a702fb`…`ae53096`.

3. **Scope splits were disciplined.** 2.6 → 2.6b + deferred manager read; 2.9/2.10 carved from review loops with entries in `deferred-work.md` (lines 691–718). Source: `deferred-work.md`.

4. **Test depth on profile gates.** `profile-audience.util.spec.ts` (+266 lines in range), `profile.service.spec.ts`, `profile.e2e-spec.ts` (24 tests at retro time). Source: git evidence + local test runs during 2.10.

5. **Manual QA fed the product.** Manager photo visible in API but not UI → 2.10 expanded (PR #89 spec, PR #90 code). Source: `spec-2-10` Intent / Manual QA gap.

### Gaps and risks (with sources)

| # | Finding | Source | Disposition |
|---|---------|--------|-------------|
| 1 | **Story 2.1 AC4 (500+ rows + permission resolution ≤2s)** has no integration test against real `access-control-service` — e2e mocks the ACS port. | `deferred-work.md` lines 636–640; `employees.e2e-spec.ts` | **defer** — track before production sign-off |
| 2 | **`employees.service.ts` concentration** (~+987 net lines in epic range). List, filters, columns, export, saved views, colleague projection share one module. | `git_evidence.py` | **accept** — monitor; split only if Epic 3+ touches same seams |
| 3 | **Early “epic done” BMAD update** (#83) before 2.6b/2.9/2.10 completed. | commit `84233ec`, sprint history | **process fix** — gate epic `done` on all story keys |
| 4 | **PP-line / FPA e2e for s3/s5** not added in 2.10; audience util covered, assembly path thinner. | Story 2.10 access-control review | **defer** — test follow-up |
| 5 | **S5 project-line “CV+certs only”** satisfied today because only `PersonCertificate` rows exist; Epic 16 document types will need `toS5` filtering by ACS restriction. | `spec-2-10` Boundaries; `profile.service.ts` `toS5` | **defer** to Epic 16 |
| 6 | **Colleague S1 photo:** matrix lists photo in colleague R; Story 2.10 AC says no avatar for colleague — UI uses `isManagementProfile` heuristic. | `section-matrix.md` S1; `EmployeeProfilePage.tsx` | **accept** — clarify normative intent in Epic 3 planning if colleague photo is required |
| 7 | **Certificate download links** use plain `<a href>` (BFF session cookie); photo uses `useAuthenticatedAssetUrl`. Same server fail-closed gate, different client pattern. | `ProfileManagementReadOnlySection.tsx` | **accept** — optional UX hardening later |

## Behavior verification

Exercised during Epic 2 delivery (not a dedicated retro run):

- `people-service`: profile unit + e2e suites (incl. manager s3/s5, project-line-only negative) — **passed** at Story 2.10 merge.
- `frontend`: Playwright management profile, colleague, saved views, export specs — **passed** for touched stories.
- **Not exercised in retro:** full local stack manual pass across all 11 stories; 500+ employee perf with live ACS.

## Previous-retro follow-through

No Epic 1 retrospective document exists. Epic 1 `action_items` in `sprint-status.yaml` remain `open` — not re-audited in this retro (out of Epic 2 scope).

## Action items

| Action | Owner | Status |
|--------|-------|--------|
| Add integration perf test: All Employees with **real** `access-control-service` batch resolve at 500+ subjects within 2s (Story 2.1 AC4 / `deferred-work.md`) | team | open |
| Add PP-line and FPA e2e for manager `s3`/`s5` profile GET (Story 2.10 review gap) | team | open |
| BMAD gate: set `epic-N: done` only when **all** epic story keys are `done` in `sprint-status.yaml` (avoid #83-style early close) | process | open |
| When Epic 16 adds non-certificate S5 types, filter `toS5` by ACS project-line restriction | Epic 16 | open |

## Acceptance verdict

**Verdict: accepted-with-open-items**

**Criteria: profiled** (no single epic-level AC block in `epics.md`; judged against Epic 2 goal in `epics.md:927–938` and all story ACs marked `done`).

**Evidence:** All 11 stories shipped and merged; access-control matrix traces updated for list, colleague, self-service, and manager read paths; known NFR and e2e gaps are documented in `deferred-work.md` and action items above — not silent failures.

**Unfinished stories:** none (`pending_stories: []`).

## Open questions

1. Is Story 2.1 AC4 a **release blocker** for a pilot, or acceptable with mocked ACS in CI until perf harness exists?
2. Should colleague profile ever show S1 photo per matrix, or is management-only photo UI the intended product reading?
