---
topic: People Management Platform UX
updated: 2026-09-01T12:18
---

- (decision by user) Sources confirmed: prd.md, ARCHITECTURE-SPINE.md, epics.md, docs/requirements/project-requirements.md, Spec_Changelog_v1.2_to_v1.5.md. No additional visuals/decks/Figma.
- (decision by user) Stakes: internal HR/people-management tool for own org - not consumer-facing, not regulated (healthcare/finance sense), though access control is the sharpest edge.
- (decision by user) Working mode: coaching path - go step by step through decisions rather than fast-path batch drafting.
- (event) Imported user-supplied full v1.5 requirements text to imports/requirements-v1.5-full.md - equivalent to base project-requirements.md + Spec_Changelog_v1.2_to_v1.5.md, kept for convenient direct reference.
- (assumption) Foundation detected from existing frontend code (services/frontend), not yet user-confirmed: responsive web (mobile drawer + desktop collapsible sidebar already built), shadcn/ui 'radix-nova' style on React 19 + Vite + Tailwind v4, Zinc base + Blue accent, OKLCH tokens, dark-mode CSS vars present in index.css but no toggle wired yet, Lucide icons, i18next English-only (RTL:false), TanStack Query + Axios via BFF only (no direct domain-service calls per AD-5), no auth yet (TODO interceptor stub), IA is a placeholder single Home sidebar item only.
- (decision by user) Dark mode: CSS variables stay ready in index.css, but the toggle/feature itself is unbuilt for this pass.
- (decision by user) Form factor: responsive web is the whole surface - no native mobile app, no other platform planned.
- (decision by user) Journey 1 confirmed - Employee self-service (protagonist: Chidi, Developer). Steps: (1) opens Home before standup, sees personalized day not static placeholder (2) completes an IT-sent security-training action item via campaign, marks it done (3) updates own personal contacts (S2) and emergency contact (S3) on the shared Employee Profile page, resolved with full write rights because it is their own profile (4) checks CDS/IDP (S12), deadline in 3 days, not yet complete - reviews external IDP doc but does not falsely mark complete (5) glances at own leaves (S10), read-only, link out to timetracker (6) flips own open-to-mentoring flag on (S13). Surfaces touched: Home (must become personalized, currently a static placeholder), the shared Employee Profile page (self-view = same page as everyone else's profile, elevated write rights when viewer is subject). No separate self-service page - 4.3 capabilities are edit rights on normal profile sections.
- (decision by user) Journey 2 confirmed - Unit Manager fulfilling a resourcing request (protagonist: Olena Romaniuk, UM, Engineering management). Steps: (1) opens UM dashboard (grouped by people), sees summary counters and 1 unfulfilled request highlighted (2) opens the request, routed to her department automatically (3) reviews her own department's people (cannot borrow from another unit) or attaches an external PeopleForce candidate - goes internal (4) submits candidate for DM approval - platform silently auto-generates a shared link scoped to evaluation view (S1,S4,S11,S12,S5-CV/certs-only; never S2,S3,S7,S8), bound to the request lifecycle not a fixed clock (5) request shows pending DM review; she moves to her own overdue action items. Surfaces added: Unit Manager dashboard, Resourcing request detail + fulfilment flow.
- (decision by user) Journey 3 confirmed - Delivery Manager reviewing/closing a resourcing request (protagonist: Diana Savchuk, DM, Delivery Management, the request's original creator). Steps: (1) opens DM dashboard (grouped by project + Unassigned bucket), sees request state now 'candidate proposed - pending review' (2) opens it - she has no Manager/Project-line access to this person yet, so what she sees IS the shared-link evaluation view (S1,S4,S11,S12,S5-CV/certs), not the normal profile (3) risk status (S6) is simply absent - the UM's call to enable it, not hers - empty state needs deliberate design, not a look-broken gap (4) approves - fills 1/1 headcount but does not auto-close (5) explicitly closes the request as a separate action - writes S15 history on both sides, kills the shared link immediately (not on a timer). Surfaces added: Delivery Manager dashboard, evaluation-view shared-link as distinct from normal profile, approve and close as two separate deliberate actions.
- (decision by user) Journey 4 confirmed - People Partner offboarding (protagonist: Oleh Boiko, Developer by title, assigned PP as a functional role - deliberately independent of job title, per 2.2/2.3). Steps: (1) opens PP dashboard (same shape as UM/DM, groupable by dept/project, NO resourcing block), sees his HR-line-assigned people (2) adds a feedback record (S8, management-only default) documenting an exit conversation (3) attempts to record the departure (4.16) - blocked because the departing person manages 3 direct reports, prompted to re-parent first with one-click default to the departing manager's own manager (4) accepts default for 2 reports, manually overrides the 3rd to a different lead (5) resubmits - departure cascades automatically: profile read-only + drops from default list (stays filterable), open action items cancel as departed, active mentorship pair auto-ends with system-generated closure note bypassing the mandatory-note gate, account deactivates, all access ends immediately (6) relationship journal (3.4) already has it logged, nothing manual. Surfaces added: People Partner dashboard, full-RW PP profile editing, record-a-departure flow including the manager-blocking/re-parent edge case.
- (decision by user) Journey 5 confirmed - HR Admin standing up an extensible functional role (protagonist: Anton Savchenko, Head of IT, holds HR Admin - the spec's own canonical example). Steps: (1) opens Administration - Functional roles (2) creates 'IT Campaign Creator' role (3) grants exactly one permission: create form campaigns (4) almost also grants 'view resourcing dashboard' out of habit, catches himself - a new functional role must never widen data access, only unlock features - unchecks it (5) assigns a colleague to the role via UI, no deploy/schema change (6) that colleague creates campaigns but still sees recipients only via plain colleague view, except their own campaign's task status once live (3.3.7) (7) Anton verifies HR Admin itself grants him zero data access - opens a colleague profile, gets the plain colleague view like anyone (8) also defines a new custom field (GitHub handle, text, colleague visibility) - immediately usable as an All Employees filter/column, no migration. Surfaces added: Administration (functional roles, permission grants, role assignment, custom field definition).
- (decision by user) Journey 6 confirmed - Risk + S7 management notes, two perspectives on one profile. Protagonist A: Viktor Bondar (Engineering Director, reporting line, multi-level transitive access, no explicit grant). Adds a risk record (S6, level high), writes a management note (S7) flagged visible-for-PM but NOT visible-for-employee - deliberate, not oversight, since both flags default closed. Protagonist B: Dmytro Danylenko (PM, project line on that project). S6 risk stays fully RW for him - project line only narrows S2/S3/S5(CV-only), NOT S6. S7 is where PM specifically differs from DM: read-only, only the visible-for-PM-flagged note - a DM would have full RW here. Employee sees neither risk nor the note (flags never set for them). Surfaces added: Risk Dashboard, risk-record creation with trend, S7 dual-flag authoring, the one place DM and PM genuinely diverge within project line.
- (decision by user) Journey 7 confirmed - Colleague browsing (protagonist: Vagif Mammadaliyev, plain colleague to almost everyone). Tries to filter All Employees by open-to-mentoring - filter is genuinely absent (not greyed out), because S13 is fully closed to colleagues; self-serve mentor-hunting isn't a colleague feature at all (4.11 gates the pool behind assign-and-end-mentorships, manager-line/PP only) - his real next step is asking his PP. Browses colleague-mode All Employees (whitelist columns only: S1, leave dates no type, project name), opens Chidi's profile (from journey 1) and sees exactly the whitelist rendered - away Aug 12-19 with no leave type, project name only, nothing else present at all (not hidden, absent from the response). Export button behaves the same as for anyone, just scoped to entitled columns. Surfaces added: All Employees colleague-mode as its own distinct whitelist view (not client-side field stripping), mentor-pool boundary as a deliberate visible absence.
- (decision by user) Design decisions confirmed: (1) Typography - activate Geist (already a dependency, unused) as the single typeface for display+body, scale = display/heading/body/label/muted (2) Brand posture: quiet, precise, trustworthy - restraint not decoration, Zinc+Blue already reads this way (3) Risk severity needs a dedicated color system - not the existing --chart-1..5 tokens (those are generic data-viz, unrelated semantically) - a green-to-purple ramp for low/need-attention/medium/high/leaver, always paired with a text label, never color alone (accessibility).
- (decision by user) Components confirmed: SeverityBadge (colored dot + label + optional trend arrow, reused everywhere risk appears), StatusPill (generic lifecycle chip for action items/requests/campaign recipients, uses existing secondary/muted/destructive tokens not the severity ramp), FlagIndicator (S7 visible-for-employee/visible-for-PM toggle+state, must read clearly as off-by-default). Layout/Elevation/Shapes: inherit shadcn/Tailwind defaults, no overrides - radius 0.625rem drives standard sm/md/lg/xl derivation.
- (event) Discovery and Design wrapped by user request. Moving to Finalize.
- (event) Spines distilled: DESIGN.md and EXPERIENCE.md written (status: draft). Input reconciled: imports/requirements-v1.5-full.md - equivalent to base+changelog, nothing dropped, see reconcile-requirements-v1.5-full.md.
- (assumption) Non-blocking open items carried into the spine text rather than blocking finalize: (1) dark-mode toggle CSS-ready but unbuilt - noted in State Patterns (2) severity-attention/severity-medium need an explicit AA contrast check once implemented - noted in Accessibility Floor (3) no authentication exists yet in the frontend scaffold - every Key Flow assumes it; wiring auth is out of this spine's scope.
- (event) Reviewer Gate: rubric walker + accessibility ad-hoc both run. 6 findings total (1 high, 2 medium, 3 low) - 5 fixed directly in EXPERIENCE.md/DESIGN.md (reduced-motion, table aria-sort/aria-live, FlagIndicator accessible name, Button Component Patterns row), 1 left open and tracked (severity-attention/medium contrast unconfirmed), 1 no-action-needed (epics.md source citation). Editorial pass (bmad-review structure+prose) found 4 items, all applied (condensed a redundant Do's-and-Don'ts line, cross-referenced Section-omission/Permission-adjacent-absence, preserved intentional cross-file repetition of color-never-alone, fixed one awkward sentence). validation-report.html/.md written and opened. Both spines set to status: final, updated 2026-08-29.
- (event) Update: mock coverage confirmed for the remaining Epic 5 dashboard trio - added mockups/key-um-dashboard.html (Olena, grouped by people, summary counters, highlighted unfulfilled request, own action items) and mockups/key-pp-dashboard.html (Oleh, grouped by department/project like DM, deliberately no resourcing/Unassigned block). DM/PM dashboard was already mocked as the shared dashboard-engine reference; Home, Resourcing, Administration, Shared-link evaluation view, and Mentorship Hub remain spine-only for now.
- (event) EXPERIENCE.md updated: linked both new mockups at Key Flows (Flow 2 - Olena, Flow 4 - Oleh), updated mock count reference from four to six, updated: 2026-09-01. No reviewer gate run for this update - both mockups reuse already-validated tokens/components (SeverityBadge, StatusPill) and the DM dashboard's established structural pattern; no new behavioral or accessibility surface introduced.
- (change) Update: reworked mockups/key-all-employees.html to fully trace 4.1's manager-mode column example list (country, join year, years-with-company derived, gender, department, grade, position, employee type, English level, risk level, mentorship status, project, custom field) and section-matrix.md's colleague-mode whitelist (S1 full read - name, position, department, country/city, manager, people partner, mentor, project; S10 dates-only no type; S11 project-name-only). Added an explicit Columns panel per mode, plus a 'never offered' disabled-chip row on colleague mode (grade/risk/mentorship-status/employee-type/English-level/leave-type/PM-DM-period/management-visibility custom field) each tagged with the closing section, to make the server-side whitelist provable rather than just asserted in a note. Table widened accordingly with horizontal-scroll container per Responsive & Platform.
- (event) User-supplied visual: Jira 'List' view screenshot (O4 board, Ocean's 4 project) provided as a UI pattern reference for making All Employees more user-friendly - checkbox multi-select, avatar-initial badges for person references, collapsible group-header rows, persistent search/filter/group toolbar, column-manager icon, 'X of Y' pagination footer with refresh.
- (decision) Adopted the Jira list's STRUCTURAL/interaction pattern for All Employees, not its visual theme: kept DESIGN.md's light Zinc+Blue tokens (dark mode stays explicitly out of scope per existing State Patterns entry) and used restrained avatars - the person's own S1 photo, or initials on a neutral muted background when no photo is set - never Jira's per-person random color-hashing, which would read as decorative flourish against the brand's own Do's-and-Don'ts. New interaction primitives added to EXPERIENCE.md: row grouping with collapsible group headers (reuses the dashboard block-header treatment), bulk checkbox-select scoped to manager mode only (colleague mode has no bulk actions to offer, so no checkboxes there), avatar badges for any person-reference field (Name, Manager, People Partner, Mentor), and pagination for the 500+-row performance requirement (4.1).
- (event) Update finalized: DESIGN.md got an Avatar component token (photo-first, initials-on-muted fallback, no per-person color-hashing). EXPERIENCE.md Component Patterns got the Avatar behavioral entry; Interaction Primitives got row grouping (collapsible group-header rows, reusing the dashboard block-header style), bulk checkbox-select (manager mode only), and pagination. mockups/key-all-employees.html rebuilt against these against the Jira-pattern reference, keeping the light Zinc+Blue theme throughout - no dark-theme adoption. Both spine files' updated date bumped to 2026-09-01; status stays final.
- (decision) User clarified scope on the Jira-look request: layout/UX only, color palette must stay exactly as-is (DESIGN.md's light Zinc+Blue, unchanged - no dark-mode activation, no per-person avatar color-hashing). Closed remaining structural gaps vs. the Jira reference in key-all-employees.html only: (1) added a quick-filter avatar-stack in the manager-mode toolbar (click a direct report's avatar to filter to them - a legitimate new affordance, not just decoration), (2) moved the column-manager icon from the toolbar to dock at the far right of the table header row itself, matching Jira's placement, (3) upgraded Risk/Mentorship/Employee-type values to filled-tint pill chips using the SAME existing severity-*/secondary/muted tokens (denser Jira-chip feel, zero new colors, still non-interactive - no dropdown chevron, preserving SeverityBadge/StatusPill's display-only rule), (4) added a Collapse-all/Expand-all link above the grouped table. Deliberately did NOT port Jira's floating bottom-left '+ Create' - the population is a seeded list with no employee-creation UI anywhere in the product (spec: no SSO/AD/employee creation).
- (event) Update finalized: key-all-employees.html got the four layout-only Jira-fidelity additions (quick-filter avatar stack, column-manager icon docked to table header, filled pill chips for Risk/Employee-type/Mentorship using existing tokens, Collapse-all link), plus the documented deliberate non-port of '+ Create'. Fixed a self-inflicted bug along the way: an in-file CSS comment contained the literal substring 'severity-*/secondary', which closed the CSS comment early and broke every rule after it - reworded to avoid the '*/' sequence, verified brace-balanced afterward. DESIGN.md/EXPERIENCE.md's color tokens are untouched; the two new Interaction Primitives entries (quick-filter avatar stack, collapse/expand-all) were already added in the prior turn.
- (change) User-supplied visual: Jira 'column manager' popover screenshot (search box + checkbox list + 'Create a field' + 'X of Y' footer), asking to replace the static Columns chip-list panel with this real interactive-looking control. Implemented in key-all-employees.html for both modes.
- (decision) Caught and avoided a real access-control leak while implementing: colleague mode's old panel showed closed fields (Grade, Risk level, etc.) as disabled/struck-through chips WITH a reason. Moving that into the actual column-picker popover would violate EXPERIENCE.md's own 'Permission-adjacent absence' rule (State Patterns) - a greyed-out option naming its closing section leaks that the section exists, same class of bug as an error message would. Fix: colleague mode's popover now lists ONLY the whitelist fields, full stop; the closed-fields list moved to a separately-styled 'design-review note' block, explicitly labeled as not part of the shipped UI, kept for reviewer traceability only. Added a new EXPERIENCE.md > Interaction Primitives entry ('Column picker') codifying this so it's not rediscovered per-story later. Manager mode's popover has no such constraint (nothing is closed to a manager for these columns) and got 'Create a field' + all 13 columns checked.
- (change) User caught a real fidelity gap: the column-manager popover was rendered permanently open inline, not hidden-until-clicked like Jira's actual icon-triggered dropdown. Fixed in key-all-employees.html by wrapping the popover in a native <details>/<summary> element (closed by default, opens on click, zero JavaScript) and anchoring it absolutely to a small gear icon docked at the table's top-right corner, matching the screenshot's placement exactly. Removed the gear icon's old <th> table-column slot (was adding a spurious extra column) and reverted colspans/row cell counts back down accordingly (manager table 14 cols, colleague table 9 cols) - verified header/data-row/colspan counts all match. The colleague-mode 'design-review note' (closed-fields list, not part of shipped UI) now sits above the table since the popover no longer occupies that space inline.
