# Daily standup — 2026-09-10

Jira: O4-163

## Questions covered

- **Ihor's S2S auth task — done** — integration and unit tests finished. The feature works but
  requires local configuration: a user with the right functional role (Unit Manager or DM) must
  exist in the DB. Without it the administration tab is not visible, which is correct behaviour
  per the spec.
- **Administration page disappeared for Olha after pulling** — after pulling Ihor's changes,
  Olha lost access to the administration page. Diagnosis: the page is now properly access-gated
  by functional role; Olha had seeded roles from the CSV, so there may be a seed/migration bug
  where the role assignment is not reaching the access-control-service. Ihor to investigate.
- **One-click local setup script abandoned** — Ihor attempted a setup script but stopped: too
  many edge cases (empty DB vs. existing users, Keycloak dependency ordering). Dimet also found
  security gaps in the Keycloak-first approach. Decision: abandon for now; document the manual
  steps instead (already partially done via `infra/bootstrap-local.ps1/.sh` from yesterday's
  pull).
- **Lika's BFF/deferred-work PR** — Lika has a PR fixing BFF service issues and deferred work
  from Story 1.7. Code review in progress locally. May not have merge permissions; will check
  and reach out to Olha if needed.
- **Olha's progress** — no time to work yesterday; pulled today and found the missing admin
  page. Prototype page still unreachable (no progress). Local-deploy skill Jira ticket created.
  Outcomes page not started.
- **Epic 2 / next demo target** — team agreed to aim for at least partial Epic 2 delivery by
  the next demo. Priority features: All Employees list and user profile. Lika noted deferred
  work risk. Ihor confirmed those are the two main Epic 2 features still pending.

## Questions not covered / carry-forward

- Unreachable prototype page — no progress; still no root cause. Carry-forward.
- Epic 1 deferred tasks: where they should live — still no decision, no owner. Fourth
  consecutive standup without resolution.
- PeopleForce spec/epic/story cleanup — carry-forward; tentative owner: Olha.
- Open PRD questions Q1/Q3/Q4/Q6 — carry-forward.
- Epic 2 formal readiness review — carry-forward.

## Plans

- **Olha**: work on local-deploy skill and outcomes page improvements (if Ihor handles the role
  resolution debug)
- **Lika**: finish BFF/deferred-work PR locally; verify merge permissions; merge
- **Ihor**: investigate role resolution / admin page visibility bug, then pick an Epic 1 task

## Decisions

- **One-click setup script abandoned** — too complex to get right (Keycloak ordering + security
  gaps found by Dimet). Manual bootstrap scripts (`infra/bootstrap-local.*`) cover the gap for
  now.
- **Epic 2 demo target** — team will try to deliver All Employees + user profile by the next
  demo. Deferred work may reduce final scope; accepted as a risk.
- **Administration page access model confirmed** — no separate "admin" permission; access is
  via functional role (Unit Manager, DM, etc.) already in the DB. The seed/migration path for
  that role needs to be verified.
