# Daily standup — 2026-09-11

Jira: O4-166

## Questions covered

- **Olha — no progress** — no time to work yesterday or today. Will try to pull latest and
  close some stories with minimal supervision today; look at real functionality on Saturday.
- **Lika — continuing Story 1.7 deferred work** — working on fixing BFF service issues
  identified as deferred work during Story 1.7. Hopes to finish today (evening). Raised concern
  about breaking things again; team advised that CI and tests are the safety net.
- **Ihor — Epic 1 done, Epic 2 nearly done** — finished Epic 1 (required before Epic 2 could
  start per BMAD rules), then completed approximately five Epic 2 tasks. Only two tasks remain
  in Epic 2 before the team can start Epic 3. Features are working with role-based access (Unit
  Manager can create saved views and export Excel; regular employees cannot). Some design and
  feature polish still needed — Ihor noted a follow-up story or epic may be warranted.
- **`docs/local-development.md` written by Ihor** — single-command startup documented. Local
  service issues (seen by Lika and others) were a local env config problem; the doc and
  `infra/start-all-local.*` scripts should resolve them.
- **Deferred work handling advice from Ihor** — only fix deferred work items that are a real
  bug or blocker for the next task; some items may already be resolved. Key workflow order for
  spec-driven stories: `/bmad-advanced-elicitation` + `/bmad-party-mode` to review the spec
  first, *then* `/bmad-code-review`, *then* `/bmad-build`. Running code review before spec
  review leads to more gaps.
- **Manual testing vs. E2E coverage** — team acknowledged that agents (Cursor, Claude) may
  claim 100% implementation while manual checks still surface bugs. E2E tests catch regressions
  but not all UX/logic issues. No specific action taken; flagged as a process improvement area.

## Questions not covered / carry-forward

- Epic 1 deferred tasks location — no decision, no owner. **Fifth consecutive standup without
  resolution.**
- Unreachable prototype page — no progress, no root cause.
- PeopleForce spec/epic/story cleanup — tentative owner: Olha.
- Open PRD questions Q1/Q3/Q4/Q6 — carry-forward.
- Epic 2 formal readiness review — carry-forward (moot if only 2 tasks remain).
- Epic 2 design/feature polish — Ihor flagged this needs a follow-up story/epic; no ticket yet.

## Plans

- **Olha**: pull latest today; ask agent what's available in the current sprint; try to close
  stories with minimal supervision; look at real functionality Saturday
- **Lika**: finish BFF/Story 1.7 deferred work fix today/tonight using the advised workflow
  order (spec review → code review → build); then create PR and merge
- **Ihor**: essentially done for the week; Epic 2 at 5/7 tasks done

## Decisions

- **BMAD workflow order confirmed**: `/bmad-advanced-elicitation` + `/bmad-party-mode` for spec
  review → `/bmad-code-review` → `/bmad-build`. Do not run code review before spec review.
- **Deferred work triage**: fix only items that are genuine bugs or blockers; do not
  mechanically fix everything the deferred list contains — some may already be resolved.
- **Epic 2 polish**: team acknowledged the current implementation needs design and feature
  improvements; a follow-up story or small epic should be created (no owner assigned yet).
